def save_memory(memory: str) -> dict:
    """
    Saves a memory.

    Args:
        memory (str): The memory to save.

    Returns:
        dict: A dictionary confirming the memory was saved.
    """
    return {"status": f"Memory '{memory}' saved."}
